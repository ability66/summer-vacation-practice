server:
  port: 8181

spring:
  datasource:
    username: root
    password: 1234
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/2023summer?usaSSL=false&allowMultiQueries=true

mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.example.summer.entity

logging:
  level:
    com:
      example:
        mapper: debug